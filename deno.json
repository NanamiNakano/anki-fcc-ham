{
  "$schema": "https://deno.land/x/deno/cli/schemas/config-file.v1.json",
  "description": "Anki flashcards for FCC ham radio license",
  "version": "0.1.2",
  "license": "MIT",
  "tasks": {
    "generate": "python3 generate.py",
    "parse": "deno run --allow-read --allow-write parse.ts",
    "build": "deno run --allow-read --allow-write parse.ts && python3 generate.py"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
